export const API = '/api';

export const PATHS = {
  AUTH: `${API}/auth`,
  USER: `${API}/user`,
  BOARD: `${API}/board`,
  COLUMN: `${API}/column`,
  CARD: `${API}/card`,
  INVITATION: `${API}/invitation`,
  COMMENT: `${API}/comment`,
  ATTACKMENT: `${API}/attackment`,
};
